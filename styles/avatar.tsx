import { StyleSheet } from "react-native"

export const avatarStyles = StyleSheet.create({
    container: {
        width : 80,
        height: 80,
        borderRadius: 40,
        objectFit: "fill",
        borderWidth: 1
    }
})