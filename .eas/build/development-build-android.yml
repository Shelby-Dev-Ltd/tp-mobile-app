build:
  name: Run dev
  steps:
    - eas/checkout
    - run:
        name: Install dependencies
        command: npm install --legacy-peer-deps
    - run:
        name: Install prebuild
        command: npm run eas-build-pre-install
    - run:
        name: Install prebuild iOS
        command: npm run eas-build-pre-install-ios